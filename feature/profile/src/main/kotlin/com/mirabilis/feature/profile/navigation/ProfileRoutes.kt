package com.mirabilis.feature.profile.navigation

/**
 * Route keys published by `:feature:profile`. The stable [PROFILE] id is the **single cross-feature
 * interface** consumed by `003-navigation` (resolved at the human merge gate).
 */
object ProfileRoutes {
    const val PROFILE = "profile"
}

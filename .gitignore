# --- IDE ---
.idea/
*.iml
.vscode/

# --- MVI Sample App ---
# Ignore build artifacts and local configuration files
/mvi-sample

# --- OS ---
.DS_Store

# --- AI agent local/credential files (per Spec Kit security notice) ---
# Local, machine-specific agent settings may hold auth tokens — never commit.
.claude/settings.local.json
# NOTE: .claude/skills/ IS committed on purpose — it is the tool adapter layer
# (ADR-0001) and part of reproducibility. Cloning + Claude Code = it just works.

# --- Android / Gradle (added ahead of the app; harmless while empty) ---
*.apk
*.aab
*.ap_
*.dex
*.class
/build/
**/build/
.gradle/
local.properties
captures/
.cxx/
*.keystore
*.jks

# --- Kotlin/Java ---
*.log
*.h.kotlin/
/out/
**/out/

# --- AI tooling (local, per-machine; written by adapters/opencode/run-ai-paced.sh) ---
/opencode.json

# --- Obsidian (commit shared vault config; ignore per-user state) ---
.obsidian/workspace.json
.obsidian/workspace-mobile.json
.obsidian/cache
.trash/
